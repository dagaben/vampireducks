/**
 * Keyboard (arrows + J) + mobile virtual joystick + jump button
 */
export class Input {
  constructor() {
    this.keys = {};
    this.joystick = { x: 0, y: 0 };
    this.jumpPressed = false;
    // Placeholder – full implementation next
  }
}
