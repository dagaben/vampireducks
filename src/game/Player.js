/**
 * Player (CatDog) controller
 * Will handle movement, jumping, collision, and garlic collection.
 */
export class Player {
  constructor(scene) {
    this.scene = scene;
    // Placeholder – full implementation coming next
  }
}
